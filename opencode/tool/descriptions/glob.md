Find files matching a glob pattern. Returns relative file paths with a summary.

Features:
- Summary header showing total match count and file type breakdown (e.g. "10 .py, 3 .ts")
- Results limited to 500 matches with clear truncation advice
- Use recursive patterns like '**/*.py' to search all subdirectories
