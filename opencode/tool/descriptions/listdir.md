List the contents of a directory. Shows files and subdirectories with type indicators.

Use this to explore project structure. Set recursive=true for a tree view (limited to 3 levels deep).
Ignored directories (.venv, __pycache__, node_modules, etc.) are automatically filtered out.
