Read the contents of a file. Use line_offset and line_count for partial reads.

The output includes line numbers in the format "LINENO:content" for easy reference.
Paths can be relative to the project root or absolute.
