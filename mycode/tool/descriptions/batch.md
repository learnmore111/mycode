Execute multiple tool calls in parallel within a single request.

Use this when you need to run several independent operations simultaneously (e.g. reading multiple files, running multiple searches). Maximum 25 calls per batch. Only built-in tools are supported (no nested batch or task).
