Write content to a file. Creates the file and parent directories if they don't exist. Overwrites existing content.

Features:
- Returns a preview snippet of the written content (first/last lines with line numbers)
- Shows file size and line count summary (old → new for overwrites)
- For partial edits, prefer the edit tool instead
