Create and manage a todo list to track progress on multi-step tasks.

Each todo item has an id, content, and status (pending, in_progress, completed, cancelled).
Use merge=true to update existing items, merge=false to replace the entire list.
